<?php if ($form->hasErrors()): ?>
    <div class="form-errors">
        <?php foreach ($form->getErrors() as $error): ?>
            <?= $this->fetch(':form/form-error', ['error' => $error]) ?>
        <?php endforeach ?>
    </div>
<?php endif ?>